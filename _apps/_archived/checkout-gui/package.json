{
  "name": "checkout-gui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build -b none && node copy-executable.js",
    "bundle": "tauri build",
    "debug": "tauri build -v -b none -d",
    "preview": "vite preview",
    "tauri": "tauri",
    "vite": "vite",
    "vite-build": "tsc && vite build"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.6",
    "typescript": "^5.0.2",
    "vite": "^4.4.4"
  }
}
